# PROGRESS — บันทึกการทำงาน

> ทุกเฟสต้องมีบันทึกที่นี่ · ใหม่สุดอยู่บนสุด
> รูปแบบบังคับ: ทำอะไร / ไฟล์ที่แตะ / commit / วิธีย้อนกลับ / สิ่งที่ยังค้าง

## วินัยที่ต้องรักษา

**engine กับ data ห้าม commit รวมกัน**
→ revert เนื้อเรื่องไม่มีวันทำ engine พัง และ revert engine ไม่มีวันทำเนื้อเรื่องหาย

**คำสั่งที่ใช้บ่อย**

```bash
git log --oneline --decorate
```

```bash
git revert <hash>
```

```bash
git checkout phase-2 -- js/engine.js
```

---

## แผน 7 เฟส

| เฟส | งาน | ประตูอนุมัติ (ต้องเห็นของจริงก่อนไปต่อ) | สถานะ |
|---|---|---|---|
| 0 | git + เอกสารกฎ | `docs/DECISIONS.md`, `docs/SCHEMA.md` | ✅ เสร็จ |
| 1 | สอบเทียบพิกัด ~70 เมือง + polygon ภูมิภาค | **`tools/calibrate.html`** เปิดแล้วเห็นจุด/เส้นทับแผนที่ถูกตำแหน่ง | ⬜ |
| 2 | Engine + UI shell + beat ตัวอย่าง 4 อัน | เว็บกด "ต่อไป" ได้จริง 4 ฉาก — **ยืนยัน schema ก่อนเขียน 60 beat** | ⬜ |
| 3 | แปลง MD → timeline.js ครบ (ใช้ Storyboard agent) | เรื่องเดินครบ 228–276 | ⬜ |
| 4 | ระบบศึก + **ศึกเกเต๋ง 1 ศึกให้เสร็จสมบูรณ์** | ศึกแรกเล่นได้ — **ล็อกสไตล์ก่อนผลิตที่เหลือ** | ⬜ |
| 5 | ศึก #2–#8 (Battle agents ขนาน) | ครบ 8 ศึก | ⬜ |
| 6 | Design pass + Historian ตรวจ | งานเสร็จ | ⬜ |

**เฟส 1, 2, 4 คือประตูที่ห้ามข้าม** — เป็นจุดที่ถ้าผิดแล้วปล่อยผ่าน จะต้องรื้อของข้างบนทั้งหมด

---

## 2026-07-25 · เฟส 0 · วางกฎและระบบ log

**ทำอะไร**
- ตรวจแผนที่ `Map_of_Three_Kingdoms_(English).jpg` → 1650×1950px ชัดเจน ครอปขยาย 3x ยังอ่านออก
  ยืนยันว่าสถานที่ในเนื้อเรื่องมีบนแผนที่ครบทุกจุด และแผนที่มีเส้นประแดง = เขตแดนปี 228 ให้ลอกได้
- อ่าน `What_If_First_Northern_Expedition.md` ครบ 233 บรรทัด (9 หัวข้อ, ค.ศ. 228–276)
- `git init` + สร้างโครงโฟลเดอร์
- เขียน `docs/DECISIONS.md` — กฎ 6 ข้อ, สี, สถาปัตยกรรม, ขอบเขต 8 ศึก
- เขียน `docs/SCHEMA.md` — สัญญาโครงสร้าง data ทุกไฟล์
- สำเนาแผนที่ไป `assets/map.jpg` (ต้นฉบับคงไว้ที่เดิม ไม่แตะ)

**การตัดสินใจที่ผู้ใช้เคาะ**
1. ศึก Tier A = **8 ศึก**
2. Subagent = **เจาะจง 3 จุด** (Storyboard เฟส 3 · Battle ขนานเฟส 5 · Historian เฟส 6) ไม่ตั้งทีมประจำ
3. ผลลัพธ์ = **โฟลเดอร์แยกไฟล์** ดับเบิลคลิกเปิดได้

**ปัญหาที่เจอและแก้แล้ว**
- แผนที่ต้นฉบับใช้ SHU=แดง WU=เขียว สลับกับที่ผู้ใช้ต้องการ
  → แก้โดย desaturate base map + ปิดทับตัวอักษร WEI/SHU/WU + ให้สีทั้งหมดมาจาก SVG layer เท่านั้น

**ไฟล์ที่แตะ**
`docs/DECISIONS.md` · `docs/SCHEMA.md` · `docs/PROGRESS.md` · `.gitignore` · `assets/map.jpg`

**commit** — `7338abd` · tag `phase-0`

**วิธีย้อนกลับ** — เฟสนี้เป็นเอกสารล้วน ไม่มีโค้ด ไม่ต้องย้อน

**ยังค้าง / ต้องตรวจในเฟสถัดไป**
- ยังไม่ยืนยันว่า **"เงียบกุ๋น" (Ye/Yecheng)** อยู่ตรงไหนบนแผนที่ — ต้องหาให้เจอในเฟส 1
- ยังไม่ยืนยันตำแหน่ง **"หนานอัน"** และ **"ซีหลิง"** — ต้องหาให้เจอในเฟส 1
